{
  "name": "gestor-llamados",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm install --prefix api && npm install --prefix front",
    "dev": "concurrently \"npm run dev --prefix api\" \"npm run dev --prefix front\"",
    "build": "npm run build --prefix front",
    "start": "npm run start --prefix api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
